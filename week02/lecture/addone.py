# read number and add one
#Author is Adrian 

#We must convert the string that input returns an int
number = int(input('please enter a number:'))
newNumber = number +1
print ('{} plus one is {}'.format(number, newNumber))
